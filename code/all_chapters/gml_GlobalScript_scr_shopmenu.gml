/// PATCH

/// AFTER
                if (menuc[15] == 0)
                {
                    snd_play(snd_locker);
                    global.gold += sellvalue;
/// CODE
                    scr_add_money(sellvalue);
/// END

/// AFTER
                if (menuc[16] == 0)
                {
                    snd_play(snd_locker);
                    global.gold += sellvalue;
/// CODE
                    scr_add_money(sellvalue);
/// END

/// AFTER
                if (menuc[17] == 0)
                {
                    snd_play(snd_locker);
                    global.gold += sellvalue;
/// CODE
                    scr_add_money(sellvalue);
/// END