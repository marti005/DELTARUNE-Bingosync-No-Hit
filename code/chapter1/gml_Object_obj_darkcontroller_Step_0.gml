/// PATCH

/// AFTER
                        newequip = global.armor[global.submenucoord[global.submenu]];
/// CODE
                        if (newequip == 4)
                            scr_add_goal_progress(75, 1);
/// END